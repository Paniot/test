import Logo from './../img/logo.png';
import './Header.css'

const Header = () => {
  return (
    <header className="header">
      <img src={Logo} alt="hmm"></img>
    </header>)
};

export default Header;